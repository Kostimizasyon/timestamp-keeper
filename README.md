TODO:

Store the last X amount of yt links with timestamp so it can act like history exactly, if the passed time is < %7 of the vid, dont store if it is in the last %10 dont store, whenver a video is finished clear the url
